# ✈️ Aviatickets - Система поиска и бронирования авиабилетов

Полнофункциональное приложение для поиска, бронирования и управления авиабилетами с мобильным приложением (React Native) и backend API (NestJS).

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Архитектура](#архитектура)
- [Технологии](#технологии)
- [Быстрый старт](#быстрый-старт)
- [Настройка базы данных](#настройка-базы-данных)
- [Настройка переменных окружения](#настройка-переменных-окружения)
- [API Документация](#api-документация)
- [Структура проекта](#структура-проекта)
- [Развертывание](#развертывание)
- [Безопасность](#безопасность)

---

## 📖 Описание проекта

Aviatickets - это комплексная система для работы с авиабилетами, включающая:

### Backend (NestJS)
- 🔐 Полная система авторизации (email/password, Google OAuth, Apple OAuth)
- ✈️ Поиск рейсов через Onelya API
- 📝 Бронирование билетов с сохранением в MongoDB
- 💳 Интеграция платежей (заглушка)
- 📄 Генерация PDF билетов
- ❓ FAQ система с категориями
- 💬 Чат поддержки
- 👤 Управление профилем пользователя

### Frontend (React Native)
- 📱 Мобильное приложение для iOS и Android
- 🔍 Поиск рейсов с фильтрацией
- 🎫 Просмотр и управление бронированиями
- 💬 Чат с поддержкой
- ❓ FAQ с поиском
- 👤 Профиль пользователя

---

## 🏗️ Архитектура

```
┌─────────────────┐
│  React Native   │  ← Мобильное приложение
│     (Expo)      │
└────────┬────────┘
         │ HTTP/REST
         │ JWT Auth
┌────────▼────────┐
│   NestJS API    │  ← Backend сервер
│   (TypeScript)  │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
┌───▼───┐ ┌──▼────┐
│MongoDB│ │Onelya │
│       │ │  API  │
└───────┘ └───────┘
```

### Основные компоненты:

1. **Auth Module** - Авторизация и аутентификация
2. **Users Module** - Управление пользователями
3. **Flights Module** - Поиск рейсов
4. **Booking Module** - Бронирование билетов
5. **FAQ Module** - Часто задаваемые вопросы
6. **Support Module** - Чат поддержки

---

## 🛠️ Технологии

### Backend
- **NestJS** - Node.js фреймворк
- **TypeScript** - Типизированный JavaScript
- **MongoDB** - NoSQL база данных
- **Mongoose** - ODM для MongoDB
- **JWT** - Аутентификация
- **bcrypt** - Хеширование паролей
- **Swagger** - API документация
- **PDFKit** - Генерация PDF

### Frontend
- **React Native** - Мобильная разработка
- **Expo** - Фреймворк для React Native
- **React Navigation** - Навигация
- **AsyncStorage** - Локальное хранилище
- **@gorhom/bottom-sheet** - Bottom sheet компоненты
- **Expo Auth Session** - OAuth авторизация

---

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ (рекомендуется 20+)
- MongoDB (локально или облако)
- npm или yarn
- Для мобильного приложения: Expo CLI

### Установка Backend

```bash
cd tickets-backend
npm install
```

### Установка Frontend

```bash
cd aviatickets-demo
npm install
```

---

## 🗄️ Настройка базы данных

### Вариант 1: Локальная MongoDB

1. Установите MongoDB:
   ```bash
   # macOS
   brew install mongodb-community
   
   # Linux
   sudo apt-get install mongodb
   
   # Windows
   # Скачайте с https://www.mongodb.com/try/download/community
   ```

2. Запустите MongoDB:
   ```bash
   # macOS/Linux
   mongod
   
   # Windows
   # Запустите MongoDB как службу
   ```

3. В `.env` укажите:
   ```
   MONGO_URI=mongodb://localhost:27017/tickets
   ```

### Вариант 2: MongoDB Atlas (Облако - РЕКОМЕНДУЕТСЯ)

1. Зарегистрируйтесь на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте бесплатный кластер
3. Создайте пользователя базы данных
4. Получите connection string:
   ```
   mongodb+srv://username:password@cluster.mongodb.net/tickets?retryWrites=true&w=majority
   ```
5. В `.env` укажите полученный connection string:
   ```
   MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/tickets
   ```

### Вариант 3: Docker

```bash
cd tickets-backend
docker-compose up -d mongo
```

MongoDB будет доступна на `mongodb://localhost:27017/tickets`

---

## ⚙️ Настройка переменных окружения

### Backend (.env файл)

1. Скопируйте пример файла:
   ```bash
   cd tickets-backend
   cp .env.example .env
   ```

2. Откройте `.env` и заполните:

```env
# База данных
MONGO_URI=mongodb://localhost:27017/tickets

# Сервер
PORT=3000
NODE_ENV=development

# JWT (ВАЖНО: измените на случайную строку!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Onelya API (получите у менеджера Onelya)
ONELYA_BASE_URL=https://test.onelya.ru/api
ONELYA_LOGIN=your_onelya_login
ONELYA_PASSWORD=your_onelya_password
ONELYA_POS=your_pos_id

# Google OAuth (опционально)
GOOGLE_CLIENT_ID=your_google_client_id
```

### Генерация JWT_SECRET

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### Frontend (app.json)

В `aviatickets-demo/app.json` укажите URL вашего API:

```json
{
  "expo": {
    "extra": {
      "apiBase": "http://localhost:3000"
    }
  }
}
```

Для production замените на URL вашего сервера.

---

## 🚀 Запуск проекта

### Backend

```bash
cd tickets-backend

# Режим разработки (с hot-reload)
npm run start:dev

# Production
npm run build
npm run start:prod
```

Сервер запустится на `http://localhost:3000`

### Frontend

```bash
cd aviatickets-demo

# Запуск Expo
npx expo start

# Для iOS
npx expo start --ios

# Для Android
npx expo start --android
```

---

## 📚 API Документация

После запуска backend, Swagger документация доступна по адресу:

**http://localhost:3000/api**

### Основные эндпоинты:

#### Авторизация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `POST /auth/google` - Google OAuth
- `POST /auth/apple` - Apple OAuth
- `GET /auth/me` - Текущий пользователь

#### Пользователи
- `GET /me` - Получить профиль
- `PUT /me` - Обновить профиль
- `POST /me/avatar` - Загрузить аватар

#### Рейсы
- `GET /flights/search` - Поиск рейсов

#### Бронирования
- `GET /booking` - Список бронирований
- `POST /booking/create` - Создать бронирование
- `GET /booking/:id` - Получить бронирование
- `GET /booking/:id/pdf` - Скачать PDF билета

#### FAQ
- `GET /faq` - Список FAQ
- `GET /faq?category=payments` - FAQ по категории

#### Поддержка
- `GET /support/messages` - Сообщения чата
- `POST /support/messages` - Отправить сообщение

#### Health Check
- `GET /onelya/health` - Проверка доступности Onelya API

---

## 📁 Структура проекта

```
aviatickets.nosync/
├── tickets-backend/          # Backend (NestJS)
│   ├── src/
│   │   ├── auth/            # Модуль авторизации
│   │   ├── users/           # Управление пользователями
│   │   ├── flights/         # Поиск рейсов
│   │   ├── booking/         # Бронирование
│   │   ├── faq/             # FAQ
│   │   ├── support/         # Поддержка
│   │   ├── schemas/        # MongoDB схемы
│   │   └── main.ts         # Точка входа
│   ├── .env.example        # Пример переменных окружения
│   └── package.json
│
└── aviatickets-demo/        # Frontend (React Native)
    ├── screens/            # Экраны приложения
    ├── components/         # Компоненты
    ├── navigation/         # Навигация
    ├── contexts/          # React Context
    ├── constants/         # Константы
    └── app.json           # Конфигурация Expo
```

---

## 🔒 Безопасность

### Реализованные меры безопасности:

1. **Хеширование паролей** - bcrypt с salt rounds
2. **JWT токены** - безопасная аутентификация
3. **Валидация данных** - class-validator
4. **Защита маршрутов** - JWT guards
5. **Безопасное хранение платежей** - только последние 4 цифры карты
6. **CORS настройки** - контроль доступа
7. **Rate limiting** - защита от DDoS (можно добавить)

### Рекомендации для production:

1. Используйте HTTPS
2. Измените JWT_SECRET на случайную строку
3. Настройте CORS правильно
4. Используйте переменные окружения для секретов
5. Регулярно обновляйте зависимости
6. Настройте мониторинг и логирование

---

## 🐳 Docker

### Запуск через Docker Compose

```bash
cd tickets-backend
docker-compose up --build
```

Это запустит:
- Backend API на порту 3000
- MongoDB на порту 27017

### Сборка Docker образа

```bash
cd tickets-backend
docker build -t aviatickets-backend .
docker run -p 3000:3000 --env-file .env aviatickets-backend
```

---

## 📱 Мобильное приложение

### Функции приложения:

1. **Поиск рейсов** - поиск с фильтрами
2. **Бронирование** - создание и просмотр бронирований
3. **Профиль** - управление данными пользователя
4. **FAQ** - часто задаваемые вопросы с поиском
5. **Поддержка** - чат с поддержкой

### Настройка OAuth:

#### Google OAuth:
1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите Google+ API
3. Создайте OAuth 2.0 Client ID
4. Добавьте в `.env`:
   ```
   GOOGLE_CLIENT_ID=your_client_id
   ```

#### Apple OAuth:
1. Настройте в Apple Developer Account
2. Настройте в `app.json` для iOS

---

## 🧪 Тестирование

### Backend тесты

```bash
cd tickets-backend
npm run test
npm run test:e2e
```

### Проверка API

```bash
# Health check
curl http://localhost:3000/onelya/health

# Регистрация
curl -X POST http://localhost:3000/auth/register \
  -H "Content-Type: application/json" \
  -d '{"fullName":"Test User","email":"test@test.com","password":"123456","phone":"+79991234567","termsAccepted":true}'
```

---

## 📊 Мониторинг и логирование

Приложение использует встроенный Logger NestJS:

- Все запросы к Onelya логируются
- Ошибки логируются с контекстом
- Предупреждения о неверных настройках

Для production рекомендуется:
- Настроить централизованное логирование (ELK, CloudWatch)
- Добавить мониторинг (Prometheus, Grafana)
- Настроить алерты

---

## 🚢 Развертывание

### На Timeweb / VPS

1. Подключитесь к серверу по SSH
2. Установите Node.js и MongoDB
3. Склонируйте репозиторий
4. Настройте `.env`
5. Запустите через PM2:
   ```bash
   npm install -g pm2
   pm2 start npm --name "aviatickets" -- run start:prod
   pm2 save
   ```

### На Render / Heroku

1. Подключите репозиторий
2. Настройте переменные окружения в панели
3. Укажите build command: `npm run build`
4. Укажите start command: `npm run start:prod`

---

## 🐛 Решение проблем

### MongoDB не подключается

- Проверьте, что MongoDB запущен
- Проверьте правильность MONGO_URI
- Проверьте права доступа пользователя

### Onelya API не работает

- Проверьте учетные данные в `.env`
- Проверьте доступность API: `GET /onelya/health`
- Проверьте логи сервера

### OAuth не работает

- Проверьте Client ID в `.env`
- Проверьте настройки в Google Cloud Console
- Проверьте redirect URIs

---

## 📝 Лицензия

Этот проект создан для демонстрационных целей.

---

## 👥 Поддержка

Для вопросов и поддержки:
- Создайте issue в репозитории
- Обратитесь к документации API: http://localhost:3000/api

---

## 🎯 Дальнейшее развитие

Планируемые улучшения:
- [ ] Полная интеграция платежей
- [ ] Push уведомления
- [ ] Email рассылки
- [ ] Админ панель
- [ ] Аналитика и отчеты
- [ ] Многоязычность

---

**Удачной разработки! ✈️**