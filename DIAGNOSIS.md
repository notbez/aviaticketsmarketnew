# 🔍 Диагностика проблемы

## Проблемы:
1. ❌ Бронирования не отображаются в "Мои билеты"
2. ❌ Профиль возвращает 404 ошибку
3. ✅ Бронирование создается успешно (сообщение появляется)

## Анализ логов:

### Лог 1: Успешное бронирование
```json
{
  "booking": {
    "id": "c733a5bb-4bc1-4ac9-bf6a-475d4d9358ee",
    "from": "SVO",
    "to": "TJM",
    "flightNumber": "SU 1502",
    "price": 17454,
    "provider": "onelya-mock",
    "status": "PENDING"
  },
  "ok": true,
  "onelya_ok": false
}
```

**Проблема:** Структура ответа `{"booking": {...}}`, а frontend ожидает `{"_id": "..."}` или `{"id": "..."}`

### Лог 2: Ошибка профиля
```
Error loading profile: [Error: Ошибка загрузки данных: 404]
```

**Проблема:** Эндпоинт `/me` возвращает 404

## 🎯 Причины проблем:

### 1. Сервер на Timeweb НЕ ОБНОВЛЕН
- Старый код контроллера бронирования
- Возможно старый JWT_SECRET
- Возможно не подключен к MongoDB Atlas

### 2. Структура ответа бронирования
Backend возвращает:
```json
{
  "booking": {
    "id": "...",
    ...
  },
  "ok": true
}
```

Frontend ожидает:
```json
{
  "_id": "...",
  "ok": true,
  ...
}
```

### 3. Эндпоинт /me не работает
- Возможно UsersModule не зарегистрирован
- Возможно JWT токен не валидируется
- Возможно пользователь не существует в базе

## 🔧 Что нужно проверить на сервере Timeweb:

Подключитесь к серверу:
```bash
ssh root@193.233.103.8
cd ~/aviatickets-backend/tickets-backend
```

### 1. Проверьте .env файл:
```bash
cat .env
```

Должно быть:
- `MONGO_URI=mongodb+srv://Misha110208:...@aviamarket.7o9kplj.mongodb.net/tickets?...`
- `JWT_SECRET=a7f3e9c2b8d4f1a6e5c9b3d7f2a8e4c1b9d5f3a7e2c8b4d1f6a9e3c7b2d8f4a1`

### 2. Проверьте версию кода:
```bash
# Проверьте дату последнего изменения контроллеров
ls -la src/booking/booking.controller.ts
ls -la src/users/users.controller.ts
```

### 3. Проверьте логи PM2:
```bash
pm2 logs aviatickets-backend --lines 100
```

Ищите:
- Ошибки подключения к MongoDB
- Ошибки "Invalid token"
- Ошибки "User not found"

### 4. Проверьте, какие роуты зарегистрированы:
```bash
pm2 logs aviatickets-backend --lines 200 | grep "Mapped"
```

Должны быть:
- `Mapped {/me, GET} route`
- `Mapped {/me, PUT} route`
- `Mapped {/booking/create, POST} route`
- `Mapped {/booking, GET} route`

### 5. Тест API напрямую на сервере:
```bash
# Авторизация
curl -X POST http://localhost:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'

# Сохраните токен и проверьте профиль
TOKEN="<вставьте_токен>"
curl http://localhost:3000/me -H "Authorization: Bearer $TOKEN"

# Проверьте бронирования
curl http://localhost:3000/booking -H "Authorization: Bearer $TOKEN"
```

## 📋 Вероятные причины и решения:

### Причина 1: Код не обновлен на сервере
**Решение:** Скопируйте обновленные файлы:
- `src/booking/booking.controller.ts`
- `src/users/users.controller.ts`
- `.env`

Затем: `pm2 restart aviatickets-backend`

### Причина 2: MongoDB не подключена
**Решение:** Проверьте MONGO_URI в .env и логи подключения

### Причина 3: Пользователь не существует в MongoDB Atlas
**Решение:** Запустите скрипт создания пользователя на сервере

### Причина 4: JWT_SECRET не совпадает
**Решение:** Обновите JWT_SECRET в .env и перезапустите

## 🎯 Следующий шаг:

Подключитесь к серверу и выполните диагностику выше. Пришлите результаты:
1. Содержимое .env (без паролей)
2. Последние 50 строк логов PM2
3. Результат теста API

Тогда я точно скажу, в чем проблема.
