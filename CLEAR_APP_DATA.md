# 🧹 Очистка данных приложения

## Проблема:
При запуске приложения вы уже авторизованы автоматически, хотя не должны быть.

## Причина:
Токен сохранен в AsyncStorage и загружается при старте.

## Решение:

### Вариант 1: Через код (РЕКОМЕНДУЕТСЯ)

1. Откройте файл `aviatickets-demo/contexts/AuthContext.js`

2. Найдите строки 56-61 (они закомментированы):
```javascript
// ВРЕМЕННО: Очистка данных при запуске (для тестирования)
// Раскомментируйте эти строки, запустите приложение один раз, затем закомментируйте обратно
// useEffect(() => {
//   (async () => {
//     await AsyncStorage.clear();
//     console.log('✓ AsyncStorage очищен');
//     setLoading(false);
//   })();
// }, []);
```

3. Раскомментируйте их (уберите `//`):
```javascript
// ВРЕМЕННО: Очистка данных при запуске (для тестирования)
// Раскомментируйте эти строки, запустите приложение один раз, затем закомментируйте обратно
useEffect(() => {
  (async () => {
    await AsyncStorage.clear();
    console.log('✓ AsyncStorage очищен');
    setLoading(false);
  })();
}, []);
```

4. Сохраните файл

5. Перезапустите Expo (нажмите `r` в терминале)

6. Проверьте в логах: должно быть `✓ AsyncStorage очищен`

7. **ВАЖНО:** Закомментируйте строки обратно (добавьте `//`)

8. Снова перезапустите Expo

### Вариант 2: Через кнопку в приложении

Добавьте временную кнопку в ProfileScreen:

```javascript
// В начале компонента
import AsyncStorage from '@react-native-async-storage/async-storage';

// В JSX, перед кнопкой "Войти"
<TouchableOpacity 
  style={styles.loginButton}
  onPress={async () => {
    await AsyncStorage.clear();
    console.log('✓ Данные очищены');
    navigation.replace('MainTabs');
  }}
>
  <Text style={styles.loginButtonText}>Очистить данные</Text>
</TouchableOpacity>
```

Нажмите кнопку, затем удалите её из кода.

### Вариант 3: Через React Native Debugger

1. Откройте React Native Debugger
2. В консоли выполните:
```javascript
AsyncStorage.clear().then(() => console.log('Cleared'));
```
3. Перезапустите приложение

## После очистки:

1. Откройте вкладку "Профиль"
2. Должны увидеть экран "Вы не авторизованы" с кнопками "Войти" и "Зарегистрироваться"
3. Нажмите "Войти"
4. Введите: test@test.com / 123456

## Проверка:

После входа:
- ✅ Профиль должен показывать ваши данные
- ✅ При перезапуске приложения вы остаетесь авторизованы (это нормально)
- ✅ Кнопка "Выйти" должна очищать данные

## Если всё равно не работает:

Проверьте, что на сервере Timeweb исправлен JWT_SECRET (см. файл FIX_NOW.txt)
