# Детальная структура проекта Aviatickets Market

## 🏗️ Общая архитектура проекта

```
aviaticketsmarketnew/
├── aviatickets-demo/          # React Native приложение (фронтенд)
├── tickets-backend/           # NestJS API сервер (бэкенд)
└── tickets-backend.tar.gz     # Архив бэкенда
```

---

## 📱 FRONTEND (React Native) - aviatickets-demo/

### 🎯 Структура папки `/screens/` (21 экран)

#### 🏠 **Основные экраны навигации**
```
screens/
├── HomeScreen.js              # Главный экран поиска авиабилетов
├── SearchScreen.js            # Альтернативный экран поиска (демо)
├── ResultsScreen.js           # Результаты поиска рейсов
└── SplashScreen.js            # Экран загрузки при запуске
```

**HomeScreen.js** - Центральный экран приложения
- Форма поиска с выбором городов (откуда/куда)
- Переключатель "в одну сторону" / "туда-обратно"
- Выбор дат через DateTimePicker
- Настройка пассажиров (1-4 человека)
- Выбор класса обслуживания (Эконом/Бизнес)
- **Выбор тарифа** (Стандартный, Субсидированный, ДФО, Трилатеральное соглашение, Калининградский)
- Интеграция с Onelya API через `/flights/search`
- Волновой SVG фон с градиентами

**ResultsScreen.js** - Отображение найденных рейсов
- Список карточек FlightCard с деталями рейсов
- Волновой header с информацией о маршруте
- Floating кнопка для просмотра raw JSON ответа
- Переход к деталям рейса FlightDetailsScreen

#### ✈️ **Экраны бронирования и билетов**
```
screens/
├── FlightDetailsScreen.js     # Детали выбранного рейса
├── BookingScreen.js           # Подтверждение бронирования
├── PassengerInfoScreen.js     # Ввод данных пассажиров
├── SeatSelectionScreen.js     # Выбор мест в самолете
├── PaymentScreen.js           # Экран оплаты
├── TicketsScreen.js           # Список забронированных билетов
└── TicketDetailsScreen.js     # Детали конкретного билета
```

**FlightDetailsScreen.js** - Подробная информация о рейсе
- Волновой header с маршрутом и ценой
- Горизонтальный скролл тарифов с деталями (багаж, питание, возврат)
- Табы: "Информация о рейсе" / "Возврат и перенос"
- Детали маршрута с временем вылета/прилета
- Анимированный blur эффект при скролле
- Кнопка "Продолжить" → PassengerInfoScreen

**BookingScreen.js** - Финальное подтверждение
- Сводка по рейсу (маршрут, дата, время, класс, места)
- Итоговая стоимость
- Интеграция с Onelya API `/onelya/order/reservation/create`
- При успехе → переход на PaymentScreen

**PaymentScreen.js** - Процесс оплаты
- Отображение суммы к оплате
- Выбор способа оплаты (пока только "Банковская карта")
- Эмуляция успешной оплаты
- Подтверждение через `/onelya/order/reservation/confirm`
- При успехе → переход к списку билетов

#### 🔐 **Экраны авторизации**
```
screens/
├── LoginScreen.js             # Вход в систему
├── SignUpScreen.js            # Регистрация нового пользователя
└── LogoutScreen.js            # Обработка выхода
```

**LoginScreen.js** - Многофункциональная авторизация
- Email/пароль форма с валидацией
- OAuth интеграция: Google, Yandex, Mail.ru, Apple (iOS)
- Обработка редиректов после авторизации
- Поддержка возврата к бронированию после входа
- Интеграция с AuthContext

#### 👤 **Экраны профиля**
```
screens/
├── ProfileScreen.js           # Главная страница профиля
├── AccountScreen.js           # Настройки аккаунта
└── SettingsScreen.js          # Общие настройки приложения
```

**ProfileScreen.js** - Центр управления профилем
- Отображение аватара пользователя (с генерацией инициалов)
- Меню навигации: Аккаунт, История платежей, Поддержка, Настройки
- Состояние "не авторизован" с кнопками входа/регистрации
- Кнопка выхода из системы

#### 💳 **Экраны платежей**
```
screens/
├── PaymentsScreen.js          # История всех платежей
```

#### 🆘 **Экраны поддержки**
```
screens/
├── SupportScreen.js           # Основной экран поддержки
├── SupportOptionsScreen.js    # Варианты обращения в поддержку
└── FaqScreen.js               # Часто задаваемые вопросы
```

#### 🔧 **Служебные экраны**
```
screens/
└── SelectCityScreen.js        # Модальный выбор города/аэропорта
```

### 📦 Остальные папки фронтенда

#### `/components/` - Переиспользуемые компоненты
```
components/
├── FlightCard.js              # Карточка рейса (билет-стиль с вырезами)
├── Input.js                   # Кастомное поле ввода
├── LoadingOverlay.js          # Полноэкранный индикатор загрузки
└── PrimaryButton.js           # Основная кнопка приложения
```

**FlightCard.js** - Стилизованная карточка рейса
- Дизайн в стиле авиабилета с боковыми вырезами
- Отображение авиакомпании, цены, маршрута
- Информация о рейсе: время, длительность, класс
- Предупреждения о пересадках и количестве мест
- Пунктирный разделитель и кнопка бронирования

#### `/navigation/` - Навигация
```
navigation/
├── BottomTabs.js              # Нижняя табовая навигация (Home, Tickets, Profile)
└── RootNavigation.js          # Корневая навигация с проверкой авторизации
```

#### `/contexts/` - Управление состоянием
```
contexts/
└── AuthContext.js             # Контекст авторизации (JWT токены, пользователь)
```

#### `/lib/` и `/services/` - API и сервисы
```
lib/
└── api.js                     # Обертка для API запросов с токенами

services/
└── authProviders.js           # OAuth провайдеры (Google, Yandex, Mail.ru)
```

#### `/constants/` и `/data/` - Данные
```
constants/
├── airports.js                # Список аэропортов с кодами IATA
└── api.js                     # API endpoints

data/
└── airports.js                # Дублирование данных аэропортов
```

#### `/assets/` - Статические ресурсы
```
assets/
├── icons/                     # Иконки табов (home, tickets, profile)
├── plane.png                  # Иконка самолета для карточек
├── home-wave.png              # Волновой фон
├── avatar-placeholder.png     # Заглушка аватара
└── [другие иконки и изображения]
```

---

## 🔧 BACKEND (NestJS) - tickets-backend/

### 🎯 Ключевые модули бэкенда

#### `/src/booking/` - Модуль бронирований
```
booking/
├── booking.controller.ts      # HTTP контроллер бронирований
├── booking.service.ts         # Бизнес-логика бронирований
└── booking.module.ts          # Модуль NestJS
```

**booking.controller.ts** - REST API для бронирований
- `POST /booking/create` - создание бронирования (с JWT защитой)
- `GET /booking` - список бронирований пользователя
- `GET /booking/:id` - получение конкретного бронирования
- `GET /booking/:id/pdf` - генерация PDF билета с штрихкодом

**booking.service.ts** - Сложная логика бронирований
- `createLocal()` - создание локального бронирования (fallback)
- `createOnelya()` - создание через Onelya API
- `create()` - публичный метод с автоматическим fallback
- Методы для работы с Onelya: `recalcOnelya()`, `confirmOnelya()`, `voidOnelya()`
- Генерация PDF билетов с русскими шрифтами и штрихкодами
- Интеграция с MongoDB для сохранения бронирований

#### `/src/onelya/` - Интеграция с Onelya API
```
onelya/
├── onelya.controller.ts       # Прокси контроллер для Onelya API
├── onelya.service.ts          # HTTP клиент для Onelya
├── onelya.module.ts           # Модуль NestJS
├── dto/                       # TypeScript типы для Onelya API
│   ├── avia-search.dto.ts     # Типы для поиска рейсов
│   └── order-reservation.dto.ts # Типы для бронирований
└── interceptors/
    └── onelya-logging.interceptor.ts # Логирование запросов
```

**onelya.controller.ts** - Прямой прокси к Onelya API
- `POST /onelya/avia/search/route-pricing` - поиск рейсов
- `POST /onelya/avia/search/brand-fare-pricing` - брендированные тарифы
- `POST /onelya/order/reservation/create` - создание резервации
- `POST /onelya/order/reservation/confirm` - подтверждение бронирования
- `POST /onelya/order/reservation/blank` - получение PDF билета
- Все остальные методы Onelya API (void, cancel, info)

**onelya.service.ts** - HTTP клиент для российской системы бронирования
- Basic авторизация с логином/паролем
- Обработка таймаутов (3 минуты для Onelya)
- Специальная обработка PDF ответов (ArrayBuffer)
- Детальное логирование запросов и ответов
- Обработка ошибок с сохранением контекста

#### `/src/flights/` - Модуль поиска рейсов
```
flights/
├── flights.controller.ts      # REST API для поиска
├── flights.service.ts         # Бизнес-логика поиска
├── flights.module.ts          # Модуль NestJS
└── direct-search.controller.ts # Прямой поиск (дополнительный)
```

**flights.service.ts** - Сложная логика поиска и обработки
- `search()` - основной метод поиска с двухэтапным процессом:
  1. **RoutePricing** - базовый поиск рейсов по маршруту
  2. **BrandFarePricing** - получение брендированных тарифов для каждого рейса
- `buildBrandFareRequestForRoute()` - построение запроса для брендовых тарифов
- `mergeRouteWithBrandFares()` - объединение результатов поиска
- `routeToCard()` - преобразование ответа Onelya в формат для фронтенда
- `extractBrandFares()` - извлечение тарифов с деталями (багаж, питание, возврат)
- `getFareInfo()` - получение детальной информации о тарифе
- Fallback на демо-данные при недоступности Onelya

#### `/src/auth/` - Модуль авторизации
```
auth/
├── auth.controller.ts         # Локальная авторизация (email/пароль)
├── oauth.controller.ts        # OAuth провайдеры
├── auth.service.ts            # JWT токены, хеширование паролей
├── oauth.service.ts           # Интеграция с Google, Yandex, Mail.ru, Apple
├── auth.module.ts             # Модуль NestJS
├── dto/                       # DTO для авторизации
└── guards/
    └── jwt-auth.guard.ts      # JWT защита маршрутов
```

#### `/src/users/` - Управление пользователями
```
users/
├── users.controller.ts        # CRUD операции с пользователями
├── users.service.ts           # Бизнес-логика пользователей
├── users.module.ts            # Модуль NestJS
└── dto/                       # DTO для пользователей
```

#### `/src/support/` - Модуль поддержки
```
support/
├── support.controller.ts      # API для обращений в поддержку
├── support.service.ts         # Обработка сообщений поддержки
└── support.module.ts          # Модуль NestJS
```

#### `/src/faq/` - База знаний
```
faq/
├── faq.controller.ts          # API для FAQ
├── faq.service.ts             # Управление вопросами и ответами
├── faq.module.ts              # Модуль NestJS
├── faq-seed.service.ts        # Заполнение базы тестовыми данными
└── seed-faq.ts                # Скрипт инициализации FAQ
```

#### `/src/schemas/` - MongoDB схемы
```
schemas/
├── booking.schema.ts          # Схема бронирований
├── user.schema.ts             # Схема пользователей
├── support-message.schema.ts  # Схема сообщений поддержки
└── faq.schema.ts              # Схема FAQ
```

#### `/src/providers/` - Абстракции провайдеров
```
providers/
├── onelya.provider.ts         # Провайдер Onelya
└── provider.interface.ts      # Интерфейс для других провайдеров
```

---

## 🔄 Интеграция фронтенда и бэкенда

### Поток поиска рейсов
1. **HomeScreen.js** → `POST /flights/search`
2. **flights.service.ts** → Onelya RoutePricing + BrandFarePricing
3. **ResultsScreen.js** ← массив карточек рейсов
4. **FlightDetailsScreen.js** → детали тарифов

### Поток бронирования
1. **BookingScreen.js** → `POST /onelya/order/reservation/create`
2. **onelya.service.ts** → Onelya Reservation/Create
3. **PaymentScreen.js** → `POST /onelya/order/reservation/confirm`
4. **TicketsScreen.js** ← список забронированных билетов

### Авторизация
1. **LoginScreen.js** → `POST /auth/login` или OAuth
2. **AuthContext** ← JWT токен + данные пользователя
3. Все защищенные запросы → Authorization: Bearer {token}

---

## 🚀 Технологический стек

### Frontend
- **React Native** + **Expo** - кроссплатформенная разработка
- **React Navigation** - навигация между экранами
- **React Context** - управление состоянием авторизации
- **React Native SVG** - волновые фоны и векторная графика
- **React Native Reanimated** - анимации (blur эффекты)
- **Expo Vector Icons** - иконки Material Design

### Backend
- **NestJS** + **TypeScript** - современный Node.js фреймворк
- **MongoDB** + **Mongoose** - NoSQL база данных
- **JWT** - авторизация и аутентификация
- **Axios** - HTTP клиент для внешних API
- **PDFKit** - генерация PDF билетов
- **bwip-js** - генерация штрихкодов

### Внешние интеграции
- **Onelya API** - российская система бронирования авиабилетов
- **OAuth провайдеры**: Google, Yandex, Mail.ru, Apple
- **Expo AuthSession** - обработка OAuth редиректов

---

## 📊 Особенности архитектуры

### Отказоустойчивость
- Автоматический fallback на локальные бронирования при недоступности Onelya
- Демо-данные при ошибках поиска рейсов
- Graceful degradation для всех внешних сервисов

### Производительность
- Batch обработка BrandFarePricing запросов (по 5 одновременно)
- Кеширование результатов поиска
- Оптимизированные MongoDB запросы

### Безопасность
- JWT токены с проверкой на каждом защищенном маршруте
- Валидация всех входящих данных через DTO
- Хеширование паролей с bcrypt
- CORS настройки для продакшена

### Мониторинг
- Детальное логирование всех Onelya API запросов
- Отслеживание времени выполнения операций
- Сохранение raw ответов провайдеров для отладки

Проект представляет собой полнофункциональную систему бронирования авиабилетов с интеграцией российского провайдера Onelya и современным мобильным интерфейсом.