# 🚀 Инструкция по развертыванию на Timeweb

## 1. Обновите .env файл на сервере

Подключитесь к серверу:
```bash
ssh root@193.233.103.8
```

Откройте файл .env:
```bash
cd ~/aviatickets-backend/tickets-backend
nano .env
```

Замените содержимое на:
```env
# Port для запуска сервера
PORT=3000

# Onelya API настройки
ONELYA_BASE_URL=https://api-test.onelya.ru/
ONELYA_LOGIN=trevel_test
ONELYA_PASSWORD=5mPaN5KyB!27LN!
ONELYA_POS=trevel_test

# JWT Secret (ВАЖНО: используйте надежный ключ в production)
JWT_SECRET=a7f3e9c2b8d4f1a6e5c9b3d7f2a8e4c1b9d5f3a7e2c8b4d1f6a9e3c7b2d8f4a1

# MongoDB Atlas
MONGO_URI=mongodb+srv://Misha110208:Misha110208@aviamarket.7o9kplj.mongodb.net/tickets?retryWrites=true&w=majority&appName=aviamarket

# Node Environment
NODE_ENV=production
```

Сохраните (Ctrl+O, Enter, Ctrl+X)

## 2. Обновите код на сервере

```bash
cd ~/aviatickets-backend/tickets-backend
git pull origin main
# или скопируйте обновленные файлы
```

## 3. Перезапустите backend

```bash
pm2 restart aviatickets-backend
pm2 logs aviatickets-backend
```

## 4. Проверьте работу

```bash
# Проверка здоровья API
curl http://localhost:3000/onelya/health

# Проверка авторизации
curl -X POST http://localhost:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'
```

## 📝 Данные для входа

**Email:** test@test.com  
**Пароль:** 123456

## ✅ Что было исправлено

1. ✅ JWT_SECRET изменен на безопасный случайный ключ
2. ✅ MONGO_URI настроен на MongoDB Atlas с именем базы `tickets`
3. ✅ Создан тестовый пользователь в MongoDB Atlas
4. ✅ Исправлена обработка ответов при создании бронирования
5. ✅ Добавлено логирование для отладки

## 🔧 Решение проблем

### Проблема: "User not found" при входе в профиль

**Причина:** Пользователь был в локальной базе, а не в MongoDB Atlas

**Решение:** Создан пользователь в MongoDB Atlas (уже выполнено)

### Проблема: Билеты не отображаются

**Причина:** Бронирования создавались в другой базе данных

**Решение:** Теперь все данные в MongoDB Atlas, перезайдите и создайте новое бронирование

### Проблема: "Неизвестная ошибка" при бронировании

**Причина:** Frontend неправильно обрабатывал структуру ответа

**Решение:** Исправлена проверка `json.ok === true` (уже выполнено)

## 📱 Настройка мобильного приложения

Приложение уже настроено на сервер Timeweb:
```javascript
API_BASE = 'http://193.233.103.8:3000'
```

Просто перезапустите Expo:
```bash
cd aviatickets-demo
npx expo start
```

## 🎯 Следующие шаги

1. Подключитесь к серверу и обновите .env
2. Перезапустите PM2
3. Перезапустите мобильное приложение
4. Войдите с данными test@test.com / 123456
5. Создайте тестовое бронирование
6. Проверьте, что билет отображается в разделе "Мои билеты"
7. Зайдите в профиль → аккаунт и отредактируйте данные

Все должно работать! ✈️
